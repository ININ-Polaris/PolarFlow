# BIT ININ 课题组 自用 GPU 资源管理器

## 构建 client

```bash
openapi-python-client generate --path '.\slurm-rest-api43.json' --config '.\openapi-python-client.yaml' --output-path '.\src\polar_flow\_vendor\' --overwrite
```